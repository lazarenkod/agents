---
name: spec-review
description: Ревью спецификации — экспертная проверка OpenAPI, AsyncAPI, ADR с рекомендациями по улучшению, соответствию best practices и подготовке к production
---

# Ревью спецификации

Экспертное ревью спецификаций с детальными рекомендациями уровня senior архитекторов AWS, Google, Stripe.

## Поддержка языков

**ВСЕ РЕЗУЛЬТАТЫ СОХРАНЯЮТСЯ В MARKDOWN НА РУССКОМ ЯЗЫКЕ**

## Процесс ревью

### Шаг 1: Чтение и анализ спецификации

Прочитать спецификацию и понять:
- Назначение и scope API/решения
- Целевую аудиторию (internal/external)
- Уровень зрелости (MVP/production)

### Шаг 2: Проверка по категориям

#### API Design (для OpenAPI/AsyncAPI)

| Критерий | Проверка |
|----------|----------|
| Naming Conventions | kebab-case paths, camelCase fields |
| HTTP Methods | Правильное использование GET/POST/PUT/PATCH/DELETE |
| Status Codes | Корректные коды ответов |
| Versioning | Стратегия версионирования |
| Pagination | Cursor-based для списков |
| Error Handling | Стандартный формат ошибок |
| Authentication | Security schemes |
| Examples | Примеры request/response |

#### Documentation Quality

| Критерий | Проверка |
|----------|----------|
| Descriptions | Все операции имеют описания |
| Examples | Все responses имеют примеры |
| Tags | Логическая группировка |
| Contact Info | Контактная информация |
| Terms of Service | Условия использования (для public API) |

#### Security

| Критерий | Проверка |
|----------|----------|
| Auth Required | Все endpoints защищены |
| Sensitive Data | Нет чувствительных данных в URL |
| Rate Limiting | Описаны лимиты |
| HTTPS | Только HTTPS |

#### ADR Quality (для ADR)

| Критерий | Проверка |
|----------|----------|
| Context | Полный контекст решения |
| Alternatives | Рассмотрены альтернативы |
| Consequences | Описаны последствия |
| Trade-offs | Явные компромиссы |

### Шаг 3: Генерация отчёта

Создать структурированный отчёт на русском языке.

## Чеклист ревью

### OpenAPI Review Checklist

```markdown
## API Design
- [ ] Paths используют kebab-case
- [ ] Ресурсы во множественном числе (/users, /orders)
- [ ] Нет глаголов в paths
- [ ] HTTP методы используются правильно
- [ ] Status codes соответствуют семантике

## Schemas
- [ ] Properties используют camelCase
- [ ] Все required fields помечены
- [ ] Есть descriptions для всех полей
- [ ] Есть constraints (minLength, maximum, etc.)
- [ ] Enum values в SCREAMING_SNAKE_CASE

## Documentation
- [ ] Info section заполнен полностью
- [ ] Все операции имеют summary
- [ ] Все операции имеют description
- [ ] Есть examples для responses
- [ ] Tags с descriptions

## Security
- [ ] Security scheme определён
- [ ] Все операции имеют security (кроме public)
- [ ] Нет sensitive data в query params
- [ ] OAuth scopes документированы

## Pagination & Errors
- [ ] List endpoints поддерживают pagination
- [ ] Error schema стандартизирована
- [ ] Все error responses документированы
```

### ADR Review Checklist

```markdown
## Структура
- [ ] Есть секция Статус
- [ ] Есть секция Контекст
- [ ] Есть секция Решение
- [ ] Есть секция Последствия

## Качество контента
- [ ] Контекст достаточно подробный
- [ ] Решение чётко сформулировано
- [ ] Рассмотрены альтернативы
- [ ] Причины отклонения альтернатив указаны
- [ ] Последствия разделены на + и -
- [ ] Указаны связанные ADR

## Governance
- [ ] Номер ADR соответствует sequence
- [ ] Дата указана
- [ ] Авторы указаны
- [ ] Ревьюеры назначены
```

## Формат отчёта

```markdown
# Ревью спецификации: [Название]

**Ревьюер:** [Имя]
**Дата:** [YYYY-MM-DD]
**Версия спецификации:** [X.Y.Z]

## Общая оценка

| Категория | Оценка | Комментарий |
|-----------|--------|-------------|
| API Design | ⭐⭐⭐⭐⭐ | Отличный дизайн |
| Documentation | ⭐⭐⭐⭐ | Не хватает примеров |
| Security | ⭐⭐⭐⭐⭐ | Все endpoints защищены |
| Consistency | ⭐⭐⭐⭐ | Мелкие inconsistencies |

**Общий балл:** 4.5/5

## Критические замечания 🔴

### 1. [Название проблемы]

**Местоположение:** `paths./users.post`

**Проблема:**
[Описание проблемы]

**Рекомендация:**
[Как исправить]

**Пример:**
```yaml
# Было
...

# Должно быть
...
```

## Важные замечания ⚠️

### 1. [Название]

**Местоположение:** [Путь]

**Описание:**
[Описание]

**Рекомендация:**
[Как улучшить]

## Рекомендации 💡

### 1. [Название]

[Описание рекомендации]

## Положительные аспекты ✅

1. [Что сделано хорошо]
2. [Что сделано хорошо]
3. [Что сделано хорошо]

## Следующие шаги

- [ ] Исправить критические замечания
- [ ] Рассмотреть важные замечания
- [ ] Применить рекомендации где возможно
- [ ] Запросить повторное ревью

## Заключение

[Краткое заключение о готовности спецификации]
```

## Примеры feedback

### API Design Feedback

```markdown
### ⚠️ Неконсистентное именование paths

**Местоположение:** `paths`

**Проблема:**
Используется смешанный стиль: `/users` (множественное число) и `/order` (единственное).

**Рекомендация:**
Используйте множественное число для всех ресурсов:

```yaml
# Было
/order:
  get: ...

# Должно быть
/orders:
  get: ...
```
```

### Security Feedback

```markdown
### 🔴 Чувствительные данные в URL

**Местоположение:** `paths./users/search`

**Проблема:**
Query parameter `password` содержит чувствительные данные:
```yaml
parameters:
  - name: password
    in: query  # ❌ Нельзя!
```

**Рекомендация:**
Передавайте чувствительные данные в теле запроса или header:

```yaml
requestBody:
  content:
    application/json:
      schema:
        properties:
          password:
            type: string
            format: password
```
```

### Documentation Feedback

```markdown
### 💡 Добавьте примеры ответов

**Местоположение:** `paths./users.get.responses.200`

**Проблема:**
Нет примеров ответа, что затрудняет понимание API.

**Рекомендация:**
Добавьте realistic примеры:

```yaml
responses:
  '200':
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/UserList'
        example:
          data:
            - id: "user-123"
              email: "ivan@example.com"
              name: "Иван Петров"
              status: "active"
          pagination:
            nextCursor: "cursor-xyz"
            hasMore: true
```
```

## Интеграция

### GitHub PR Comment

```yaml
# .github/workflows/spec-review.yml
name: Specification Review

on:
  pull_request:
    paths:
      - 'specs/**'
      - 'docs/adr/**'

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run automated review
        id: review
        run: |
          # Запуск линтера и генерация отчёта
          npx @stoplight/spectral-cli lint specs/openapi.yaml \
            --format markdown > review.md

      - name: Comment on PR
        uses: actions/github-script@v6
        with:
          script: |
            const fs = require('fs');
            const review = fs.readFileSync('review.md', 'utf8');

            github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: '## 🔍 Automated Specification Review\n\n' + review
            });
```

## Примеры использования

```
/spec-review specs/openapi.yaml
/spec-review docs/adr/0042-event-sourcing.md
/spec-review --detailed specs/asyncapi.yaml
```
